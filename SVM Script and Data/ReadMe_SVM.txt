The single variable method is used for generate a representative year using JSD, SY(MN,MD,MP) methods.

The input datasets (demo) have been included within the folder.

The IPYNB shows a demonstration for startyear = 2030, endyear = 2040 for one of the demo datasets.

Some example outputs (obtained through using this script) have also been kept in the folder as example outputs.

